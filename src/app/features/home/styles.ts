import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  item: {
    paddingHorizontal: 16,
    paddingVertical: 20,
    borderBottomWidth: 1,
    borderColor: 'gray',
  },
});
