import { Dimensions } from 'react-native';

export const PIE_RADIUS = 120;

export const PIE_STROKE_WIDTH = 30;

export const CANVAS_WIDTH = Dimensions.get('window').width - 32;

export const CANVAS_HEIGHT = PIE_RADIUS * 2 + 50;
